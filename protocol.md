# Proposition de protocol

## Protocol embarqué

## Interface avec IA

Module -> Command (classe mère abstraite)
`package_cmd()`

Une commande par action du robot: Bouger(x, y, t), Préhenseur(status), CameraPosition(theta_x, theta_y), DEL(couleur), 
